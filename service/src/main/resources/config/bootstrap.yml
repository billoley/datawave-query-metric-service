spring:
  application:
    name: query-metrics
  cloud:
    bus:
      enabled: false
    consul:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
  main:
    banner-mode: "OFF"
